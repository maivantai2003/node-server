name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # chạy khi push lên nhánh main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Bước 1: Lấy code
      - name: Checkout code
        uses: actions/checkout@v3

      # # Bước 2: Cài Docker
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2

      # # Bước 3: Login DockerHub (cần lưu secret)
      # - name: Login to DockerHub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # # Bước 4: Build & Push backend
      # - name: Build and push backend
      #   run: |
      #     docker build -t myapp-backend:latest ./backend
      #     docker tag myapp-backend:latest mydockerhub/myapp-backend:latest
      #     docker push mydockerhub/myapp-backend:latest

      # # Bước 5: Build & Push frontend
      # - name: Build and push frontend
      #   run: |
      #     docker build -t myapp-frontend:latest ./frontend
      #     docker tag myapp-frontend:latest mydockerhub/myapp-frontend:latest
      #     docker push mydockerhub/myapp-frontend:latest

      # # Bước 6: Deploy lên server qua SSH
      # - name: Deploy to Server
      #   uses: appleboy/ssh-action@v0.1.8
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /home/ubuntu/myapp
      #       git pull origin main
      #       docker-compose pull
      #       docker-compose up -d --build
